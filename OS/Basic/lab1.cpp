#include <iostream>
#include <cstdlib> // For system()

using namespace std;

int main() {
    // Example command: opening Notepad (Windows)
    // system("notepad");

    // Example command: list files in current directory (Windows)
    system("dir");

    return 0;
}
